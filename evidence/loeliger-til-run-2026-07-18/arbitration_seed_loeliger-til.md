# Arbitration seed — loeliger-til
Boot state for the harvest arbitration layer ONLY. Never injected into the
brief or any worker packet (native-seeds law). Seeds rank tiebreak rung 2;
in-source evidence outranks every seed.

## NEXT  [discovery-forecast]
- surface_form: NEXT
- expected_shape: fan-out
- home_ranges: §2 (concept: address interpreter), §3 (inner interpreter routine), §5 (routine impl), §6 (keyword entry)
- operator_lean: pre-open fan-out (triple-status confirmed)
- exit_exam: if §6 dictionary has a NEXT entry AND §3 defines NEXT as the inner-interpreter primitive with distinct local context, the fan-out is confirmed; a single merged sense across all ranges kills it.

## DUP  [discovery-forecast]
- surface_form: DUP
- expected_shape: fan-out
- home_ranges: §6 (stack keyword entry), §2/§3/§7 (prose/asm usage)
- operator_lean: pre-open fan-out
- exit_exam: DUP appears as a defined §6 keyword_entry with Code/Bytes AND as an inline stack-op mention in ≥2 other chapters -> fan-out confirmed; keyword-only -> killed.

## EXECUTE  [discovery-forecast]
- surface_form: EXECUTE
- expected_shape: fan-out
- home_ranges: §2/§3 (inner-interpreter concept), §6 (keyword entry)
- operator_lean: pre-open fan-out
- exit_exam: distinct concept-sense in §3 prose vs keyword-sense in §6 -> confirmed; else killed.

## SEMI  [discovery-forecast]
- surface_form: SEMI (the ;S / return primitive)
- expected_shape: fan-out
- home_ranges: §2/§3 (return-to-interpreter concept), §6 (keyword entry)
- operator_lean: pre-open fan-out
- exit_exam: SEMI/";S" defined as primitive in §3/§5 AND listed in §6 -> confirmed.

## TOKEN  [discovery-forecast]
- surface_form: TOKEN
- expected_shape: fan-out
- home_ranges: §5 (token extracting routine), §6 (keyword entry), §3 (concept)
- operator_lean: pre-open fan-out
- exit_exam: §5 "Token Extracting" routine-sense distinct from §6 entry -> confirmed.

## DROP / COLON  [discovery-forecast]
- surface_form: DROP, COLON (":")
- expected_shape: watch (lower confidence)
- home_ranges: §6 keyword entries + scattered prose
- operator_lean: pre-open (batch with the above per operator's aggressive-fan-out ruling)
- exit_exam: COLON as the ":" defining-word concept vs a literal ":" entry; DROP entry vs prose mention.
