# Arbitration seed — rappers-handbook

Boot state for the harvest arbitration layer. Rung 2 of the tiebreak ladder
(discovery conventions + forecast). In-source evidence outranks every seed here.
NEVER injected into the harvest brief or any worker packet.

Operator tiebreak preference for the craft-term-polysemy class on this source:
**split freely — pre-open fan-outs** [operator-decided].

---

## flow  [discovery-forecast]
- surface form: `flow` (also `flowing`, `flows`)
- expected shape: fan-out
- mechanical basis: dispersion 17 distinct TOC units, 110 occurrences — highest on the watchlist.
- TOC-derived home ranges:
  - craft sense (rhythmic delivery over a beat): p98–p109 "A Guide to Flow", p110–p121 "History of Flow"
  - plain-verb sense (to rap continuously): p180–p185 "On the Mic"
  - non-craft/physiological sense: p180–p185 "On the Mic"
- operator lean: **no opinion** (asked and abstained — recorded as abstention, not as the session's lean)
- exit exam: CONFIRMED if a harvest span in p180–p185 yields a `flow` mention that cannot be
  resolved to the craft sense taught in p98–p109. KILLED if every p180–p185 occurrence resolves
  to the craft sense on inspection, i.e. the collision signal was a stemming artifact.

## bar  [discovery-forecast]
- surface form: `bar` / `bars`
- expected shape: fan-out
- mechanical basis: dispersion 6, 17 occurrences. Lower rank than `flow`; seeded because the
  operator's class ruling licenses pre-opening on mechanical evidence alone.
- TOC-derived home ranges:
  - unit-of-song-structure sense ("16 bars"): p199–p208 "How to Write a Song", p14–p16 "Cipher Etiquette 101"
  - single-lyric-line sense: p39–p53 "Multis", p32–p34 "Rhyme Scheme"
- operator lean: **no opinion** (asked and abstained)
- exit exam: CONFIRMED if a span in p199–p208 uses `bar` as a countable song-structure unit while
  a span in p39–p53 uses it for one written line. KILLED if the book consistently uses one sense
  and the other reading came only from quoted lyrics rather than the book's own teaching voice.

---

## Downgraded — recorded, not deleted

`verse` (dispersion 21, count 86) and `spit` (dispersion 20, count 86) ranked ABOVE `flow` on
dispersion and were downgraded on close-read: both are the book's house vocabulary, used in a
single stable sense throughout. NOT seeded.

Finding worth carrying to the next source: **on a house-vocabulary source, dispersion measures
ubiquity, not ambiguity.** The two top-ranked rows by dispersion were both false positives; the
signal that actually predicted trouble was collision (distinct local contexts in distant ranges),
not spread. If this repeats on a second source, the probe should rank on collision and use
dispersion only as a tiebreak.
