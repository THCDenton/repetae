# Session checkpoint — 2026-07-12c (AMENDMENT SESSION: charter amendment v1.2 ratified; reference layer re-synced)

Purpose: boot file for the next workshop session. Files are the only memory.
Written under checkpoint schema v2.1 (five mandatory slots, boot manifest
last). Reads alongside 07-12 and 07-12b; ratifies what they proposed.

## What this session was

The **charter amendment session** recommended by 07-12 and re-confirmed by
07-12b — a chartered, **fully booted** workshop session (verification loop
run; report below). The operator ratified the presented docket **wholesale**
after an itemized explanation pass and an arguments-per-item pass (both
in-transcript; the amendment file is the durable record). Session class:
engine (the amendment is the session's one engine-class artifact — a
versioned charter event) + the full deferred reference-doc debt paid at
close in lockstep.

## Verified state changes

1. **`engine_charter_amendment_v1_2.md` SHIPPED and RATIFIED** (operator,
   item-by-item docket, wholesale confirmation). Twelve items + one
   workshop-procedure rider. Headlines: harvest topology UNDER EXPERIMENT
   (fold vs E/P/J/A DAG, gated on the topology experiment; placement =
   workshop builds kit, ops runs against the sealed JJU key);
   scripts-never-similarity (three-citations) law RATIFIED
   topology-independent, dissolving `residue_heuristics`; harvest autonomy +
   tiebreak ladder + web license (three guards) RATIFIED; index/shard state
   model + JSON-in-flight/prose-at-rest RATIFIED; router reduced to a
   switch; Stage 0 intake screening RATIFIED (counsel hour gates third-party
   intake only); conditional fates table for merge/map/residue; discovery
   v2.5 direction RATIFIED; diagnostics (surprise rate; "loud and cheap,
   never silent") RATIFIED; tenancy deprioritization recorded.
2. **`pipeline_config_schema.md` patched** (versioned event, recorded in its
   header): `residue_heuristics` removed from §3.5/§5/Appendix A;
   `span_size`/`span_overlap` marked RETIRED-as-constants pending the
   schema-v2 session.
3. **`workshop_primer_v3.md` SHIPPED; v2 RETIRED.** Three additions: the
   un-booted design-chat law; intel-request-first; index/mount latency
   handling (W9) in the boot loop.
4. **Lexicon + diagram lockstep patches applied at close** (delta in each
   doc's changelog/banner; enumerated in reference-doc debt below as PAID).
5. **Lexicon 07-11c gap repaired.** The mount served the stale 07-11b
   lexicon at boot; the 07-12c re-issue was rebuilt from the 07-11b mount
   base + index-verbatim 07-11c content + three marked reconstructions (see
   the lexicon changelog's RECONSTRUCTION RECORD and Ground truth below).
6. **Worklist state (charter §9 as amended, v1.2 item 11):** next items are
   the topology experiment kit + scorer (workshop side), discovery v2.5
   derivation, ambiguity-probe script; extract/judge vs integrator prompt
   derivations are gated on the experiment; config-schema-v2 session owed.

## Artifacts shipped this session (operator: download + file into workshop project)

1. `engine_charter_amendment_v1_2.md` — engine artifact (versioned charter
   event). ACCUMULATES alongside charter v1 + amendment v1.1.
2. `pipeline_config_schema.md` — engine artifact, patched. REPLACES prior copy.
3. `workshop_lexicon_v1.md` — reference artifact, re-issued (07-11c splice +
   07-12c delta). REPLACES prior copy — filing this moots the stale-mount
   question.
4. `system_diagram_v1.html` — reference artifact, revised. REPLACES prior copy.
5. `workshop_primer_v3.md` — reference artifact, versioned event. REPLACES
   `workshop_primer_v2.md` (retire v2 from project knowledge).
6. `session_checkpoint_2026-07-12c.md` — this file. ACCUMULATES.

## Rulings (this session)

- **The full amendment docket RATIFIED** [operator-decided, wholesale on an
  itemized docket]: v1.2 items 1–12 + the primer rider. The amendment file
  is the authoritative enumeration; nothing is restated here to avoid drift.
- **Experiment placement** [operator-confirmed]: workshop builds the kit +
  scorer; ops runs where the answer key lives (JJU project or scratch ops
  project, key sealed). Charter §2 controlling.
- **Amendment numbering v1.2** [operator-confirmed, clerical].
- **Lexicon reconstruction method** [session-decided, flagged here for
  review]: rebuilt rather than requesting a mid-session refile; every
  index-verbatim splice and each of the three non-verbatim reconstructions
  is enumerated in the lexicon changelog. If the operator still holds the
  true 07-11c download, a diff against this re-issue is a cheap audit —
  optional, not owed.

## Ground truth learned this session

- **W9, third observation, and the window is longer than believed.** The
  mount served the 07-11b lexicon at boot a full day+ after the 07-11c
  refile, while `project_knowledge_search` returned the 07-11c markers
  (including the exact pinned changelog line). Per the 07-12 rule the index
  won; the lag was logged; the boot proceeded. New fact: "during and shortly
  after refile" understates the lag window. Primer v3 codifies the handling.
- **The boot verification loop caught its second live filing/mount defect**
  in two booted sessions. Schema v2.1 continues to earn its slot.
- **Wholesale ratification of a large docket worked** because it followed
  two full explanatory passes (itemized plain-language, then
  arguments-with-confidence-labels). Pattern worth repeating for future
  multi-item ratifications: docket → explanation → arguments → ruling.

## Dependency ledger (verified-how) — schema v2 slot

Boot verification loop ran in full this session (un-booted 07-12b claims
re-verified as required):

| Dependency | Status | Verified how |
|---|---|---|
| `workshop_primer_v2.md` (v2, 07-11c) | ✓ | header line exact match, `view`, boot |
| `workshop_lexicon_v1.md` | ⚠ mount stale (07-11b); **verified via index** | grep showed no 07-11c content on mount; index returned the exact pinned changelog line + 07-11c entries; W9 rule applied |
| `system_diagram_v1.html` (07-11c) | ✓ | banner marker exact match, extracted from HTML |
| `session_checkpoint_2026-07-12.md` | ✓ | ends with schema-v2.1 manifest + latency note |
| `session_checkpoint_2026-07-12b.md` | ✓ | newest date-suffix on mount; header "design chat"; ends with manifest; matches operator-pasted copy |
| `harvest_topology_proposal_laymans_guide.md` | ✓ | all three markers: footer evidence label; bracket appendix; Phase 1 chunk-plan bullet |
| `engine_charter.md` + `engine_charter_amendment_v1_1.md` | ✓ | read in full this session |
| `pipeline_config_schema.md` | ✓ | read §§ relevant + patched with unique-anchor assertions |
| `harvest_map_v1.md`, `harvest_residue_v1.md`, both kit specs, `harvester_prompt_v1.md` | ✓ present | `ls` probe; consulted via index for splice content |
| `glossary_index_v31.md` (experiment answer key) | ✗ ABSENT from this mount, **by design** | flagged at docket item (a); lives JJU-side; experiment runs ops-side per §2 |
| Known-absent carried: `sources.md`, Schemer master doc, JJU source, plain 07-10 checkpoint | ✗ | unchanged, unblocking |

## Propagation / blast-radius log — schema v2 slot

- **Amendment v1.2** → charter §§3/4/5/6/7/9/10 (via appended amendment, not
  body edits); config schema (patched this close); lexicon + diagram
  (patched this close); primer (v3 this close); every future harvest-stage
  derivation (reads the amendment before the v1 §4 harvest row).
- **Topology experiment** → new worklist item (workshop kit + scorer); an
  ops run JJU-side; the CONDITIONAL fates table auto-applies on its result;
  extract/judge vs integrator derivations unblock on it.
- **Screening** → future ops intake flow; ToS (external); counsel hour
  (external, gates third-party intake only); `sources.md` registration gate.
- **Wire schema** → config-schema-v2 session (first tenant); lint gate
  design; kit scorers (JSON fixtures); router switch field
  (`verdict.status`).
- **Primer v3** → every future boot; retire v2 from project knowledge.

## Open design questions register — as amended (v1.2 item 12 is authoritative)

New/standing: topology experiment design (span choice, diff metric, pass
criteria, run logistics); judge tier ladder + punt-upward budget; wire
schema v0 field set + versioning; seeded-forecast bias check; zero-sighting
sanity flag. Carried: parked-shard aging; registry snapshot cadence; cards
design; linter generalization; target models per stage; reference-layer form
(W1 + W9 now standing exhibits); output shape; reference-artifact version
suffix; prompt-via-failures method. Dissolved this session:
`residue_heuristics`; global span constants; router delivery UX; terminal-Q
handling; JJU escalation-volume intel; fold-parallelism question (all per
07-12b, now ratified).

## Reference-doc debt — schema v2 slot

**PAID at this close, in lockstep:** lexicon (07-11c splice + full 07-12c
delta — ~24 entries minted/updated, §5 statuses, §8 rows, §9 pair 11a, §10
W9, changelog); diagram (banner, screener node + edges, S1 cluster UNDER
EXPERIMENT, coin-flips edge removed per autonomy, H8/H9 stamps, six ledger
rows, inventory update); primer v3 (three riders); config schema (item 2
consequence). **Owed: nothing.** The reference layer is in lockstep with
amendment v1.2 at this close.

## Recommended next session

**Topology experiment kit derivation** (workshop side): span choice, diff
metric, pass criteria, scorer script, run instructions for the ops ctx —
the gate everything conditional waits on. It is a kit/scorer build (dev
work, dry-testable against fixtures; the sealed key stays JJU-side per §2).
Strong alternative if the operator prefers a lighter session first:
**discovery v2.5 derivation** (fully specified direction in v1.2 item 8,
no experiment dependency). The extract/judge (or integrator) prompt
derivations remain BLOCKED on the experiment by design.

Boot: paste `workshop_primer_v3.md` → run its verification loop against the
boot manifest below → lexicon → diagram → this checkpoint (+ 07-12 and
07-12b for depth) → `engine_charter.md` + BOTH amendments →
`harvest_topology_proposal_laymans_guide.md` (the experiment derives against
its phases) → both kit specs (the experiment reuses their fixtures).

## Session hygiene / corrections ledger (own errors)

- **Lexicon rebuilt from index rather than requesting a refile.** Judgment
  call, flagged: a mid-session refile (refile = remount) would have given
  byte-certain 07-11c content; I chose reconstruction to avoid stalling a
  ratified close on a round trip, with every splice source enumerated and
  three reconstructions marked. The risk (silent divergence in the three
  reconstructed fragments) is bounded and loudly labeled — consistent with
  "loud and cheap, never silent," but a refile would have been *silent-proof*.
  If the diff audit (Rulings) finds drift, correct at next close and record
  a wart.
- **Wholesale ratification recorded as such.** The operator ruled "agreed,
  go ahead" on the full docket after two explanation passes; no item was
  ruled individually. Legal (operator is sole ratifier and chose the form),
  recorded for the audit trail with pointers to the two in-transcript passes.
- **No other defects known.** Boot loop ran clean apart from the W9 event;
  all patch scripts used unique-anchor assertions (every edit either matched
  exactly once or aborted loudly).

## Boot manifest — schema v2.1 slot (pins the next boot)

Verify each doc by opening it and matching the content-level marker.
Filenames are never version evidence. On stale-mount vs index disagreement,
apply primer v3's W9 rule (index marker hit wins; log the lag).

| Doc | File | Content-level revision marker |
|---|---|---|
| primer | `workshop_primer_v3.md` | header: "Workshop Session Primer — v3, issued 2026-07-12c" |
| lexicon | `workshop_lexicon_v1.md` | last changelog line dated "2026-07-12c" and containing "RECONSTRUCTION RECORD" |
| diagram | `system_diagram_v1.html` | banner contains "Revised 2026-07-12c (amendment v1.2):" and ends "pending the gate." (the phrase "suspended v1 shape" sits inside an `<em>` immediately before it — grep the two fragments separately) |
| charter amendment | `engine_charter_amendment_v1_2.md` | header "Engine Charter — Amendment v1.2"; status line "ratified 2026-07-12" |
| config schema | `pipeline_config_schema.md` | header contains "Amended 2026-07-12 (charter amendment v1.2, item 2)" |
| checkpoint (this session) | `session_checkpoint_2026-07-12c.md` | newest date-suffix on the mount; ends with this manifest |
| checkpoint (depth) | `session_checkpoint_2026-07-12b.md` | header contains "design chat"; ends with its manifest |
| topology exhibit | `harvest_topology_proposal_laymans_guide.md` | footer evidence label; bracket appendix; Phase 1 chunk-plan bullet |

Mismatch → primer v3's rules: older = flag before work; newer = find the
later checkpoint and re-anchor; stale primer = the boot's one hard stop.
